# =============================================================================
# FICHIER DE CONFIGURATION ENVIRONNEMENT — .env.example
# =============================================================================
#
# 📖 TEMPLATE DE CONFIGURATION
#
# Ce fichier sert de MODÈLE pour créer votre fichier .env.
# Il contient des valeurs d'EXEMPLE (factices) pour toutes les variables.
#
# ✅ UTILISATION :
#   1. Copier ce fichier : cp .env.example .env
#   2. Éditer .env avec vos vraies valeurs
#   3. Vérifier que .env est dans .gitignore
#   4. Committer .env.example (sans secrets réels)
#
# 🔒 SÉCURITÉ :
#   - Ce fichier NE CONTIENT PAS de vraies valeurs sensibles
#   - Il peut être versionné sur Git sans danger
#   - Les valeurs sont des EXEMPLES de format attendu
#
# 📝 DOCUMENTATION :
#   - Chaque variable est documentée avec son rôle et format
#   - Les valeurs factices montrent le format attendu
#   - Consultez la documentation du projet pour plus de détails
#
# =============================================================================

# =============================================================================
# 🌍 ENVIRONNEMENT GÉNÉRAL
# =============================================================================

# Type d'environnement : development, staging, production
ENV=development

# Nom de l'application
APP_NAME="Enseignement"

# Version de l'application
APP_VERSION=1.0.0

# Niveau de verbosité des logs : DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=DEBUG

# Format des logs : json, text
LOG_FORMAT=text

# Timezone
TZ=Europe/Paris

# Langue par défaut
DEFAULT_LOCALE=fr_FR

# =============================================================================
# 🔐 SÉCURITÉ
# =============================================================================

# Clé secrète (générer avec : openssl rand -base64 32)
# EXEMPLE FACTICE : changez-moi-en-production
SECRET_KEY=votre-cle-secrete-ici-generee-avec-openssl

# Clé de chiffrement AES-256 (générer avec : openssl rand -hex 32)
# EXEMPLE FACTICE : 64 caractères hexadécimaux
ENCRYPTION_KEY=0123456789abcdef0123456789abcdef0123456789abcdef0123456789abcdef

# Salt pour mots de passe (générer avec : openssl rand -base64 16)
# EXEMPLE FACTICE
PASSWORD_SALT=votre-salt-ici-base64

# Durée de validité JWT (en secondes : 3600 = 1h)
JWT_EXPIRATION=3600

# Origines CORS autorisées (séparées par virgules)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173

# Forcer HTTPS (production : true)
FORCE_HTTPS=false

# =============================================================================
# 🌐 SERVEUR WEB
# =============================================================================

# Hôte (127.0.0.1 = localhost, 0.0.0.0 = toutes interfaces)
HOST=127.0.0.1

# Port du serveur
PORT=5000

# URL publique de l'application
APP_URL=http://localhost:5000

# URL de l'API
API_URL=http://localhost:5000/api

# Timeout requêtes (secondes)
REQUEST_TIMEOUT=30

# Nombre de workers
WORKERS=4

# Mode debug (production : false)
DEBUG=true

# =============================================================================
# 🗄️ BASE DE DONNÉES
# =============================================================================

# Type : postgresql, mysql, sqlite, mongodb
DB_TYPE=postgresql

# URL de connexion complète
# Format PostgreSQL : postgresql://user:password@host:port/database
# EXEMPLE FACTICE
DATABASE_URL=postgresql://votre_user:votre_password@localhost:5432/votre_db

# OU paramètres individuels :
DB_HOST=localhost
DB_PORT=5432
DB_NAME=votre_db
DB_USER=votre_user
DB_PASSWORD=votre_password

DB_CHARSET=UTF8
DB_POOL_MIN=2
DB_POOL_MAX=10
DB_CONNECT_TIMEOUT=10
DB_LOG_QUERIES=true
DB_AUTO_MIGRATE=true

# =============================================================================
# 📦 CACHE (Redis)
# =============================================================================

# URL Redis : redis://[:password@]host:port/db
# EXEMPLE FACTICE
REDIS_URL=redis://localhost:6379/0

# OU paramètres individuels :
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# Durée de vie cache (secondes)
CACHE_TTL=3600

# Préfixe des clés
CACHE_PREFIX=enseignement:

# =============================================================================
# 📧 EMAIL / SMTP
# =============================================================================

# Serveur SMTP
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587

# Credentials (Gmail : utiliser App Password)
# EXEMPLE FACTICE
SMTP_USER=votre-email@gmail.com
SMTP_PASSWORD=votre-app-password-16-caracteres

SMTP_USE_TLS=true

# Expéditeur
MAIL_FROM_NAME="Enseignement App"
MAIL_FROM_ADDRESS=noreply@example.com

# Email admin
ADMIN_EMAIL=admin@example.com

# =============================================================================
# ☁️ STOCKAGE FICHIERS
# =============================================================================

# Type : local, s3, azure, gcs
STORAGE_TYPE=local

# Chemin local
STORAGE_LOCAL_PATH=./uploads

# AWS S3 (EXEMPLES FACTICES)
AWS_ACCESS_KEY_ID=AKIAIOSFODNN7EXAMPLE
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
AWS_REGION=eu-west-3
AWS_S3_BUCKET=votre-bucket-s3

# Azure Blob (EXEMPLES FACTICES)
AZURE_STORAGE_ACCOUNT=votrecompte
AZURE_STORAGE_KEY=votre-cle-azure-base64==
AZURE_CONTAINER_NAME=uploads

# Google Cloud Storage (EXEMPLES FACTICES)
GCS_PROJECT_ID=votre-projet-gcs
GCS_BUCKET_NAME=votre-bucket-gcs
GCS_CREDENTIALS_PATH=./credentials/gcs-key.json

# =============================================================================
# 🔗 SERVICES EXTERNES / APIs
# =============================================================================

# OpenAI (EXEMPLE FACTICE)
OPENAI_API_KEY=sk-proj-abc123xyz789exemple
OPENAI_MODEL=gpt-4-turbo
OPENAI_MAX_TOKENS=2000

# Stripe (EXEMPLES FACTICES)
STRIPE_PUBLIC_KEY=pk_test_51Abc123xyz
STRIPE_SECRET_KEY=sk_test_51Abc123xyz
STRIPE_WEBHOOK_SECRET=whsec_abc123xyz

# SendGrid (EXEMPLE FACTICE)
SENDGRID_API_KEY=SG.abc123xyz789exemple

# Twilio (EXEMPLES FACTICES)
TWILIO_ACCOUNT_SID=ACabc123xyz789
TWILIO_AUTH_TOKEN=abc123xyz789exemple
TWILIO_PHONE_NUMBER=+33612345678

# Google Maps (EXEMPLE FACTICE)
GOOGLE_MAPS_API_KEY=AIzaSyAbc123Xyz789Exemple

# Sentry monitoring (EXEMPLE FACTICE)
SENTRY_DSN=https://abc123xyz@o123456.ingest.sentry.io/7654321

# =============================================================================
# 🔑 AUTHENTIFICATION OAUTH / SOCIAL LOGIN
# =============================================================================

# GitHub OAuth (EXEMPLES FACTICES)
GITHUB_CLIENT_ID=abc123xyz789exemple
GITHUB_CLIENT_SECRET=0123456789abcdef0123456789abcdef01234567
GITHUB_CALLBACK_URL=http://localhost:5000/auth/github/callback

# Google OAuth (EXEMPLES FACTICES)
GOOGLE_CLIENT_ID=123456789-abc123xyz.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-abc123xyz789exemple
GOOGLE_CALLBACK_URL=http://localhost:5000/auth/google/callback

# Microsoft OAuth (EXEMPLES FACTICES)
MICROSOFT_CLIENT_ID=abc12345-1234-5678-9abc-def012345678
MICROSOFT_CLIENT_SECRET=abc123~xyz789exemple
MICROSOFT_CALLBACK_URL=http://localhost:5000/auth/microsoft/callback

# =============================================================================
# 📊 MONITORING & ANALYTICS
# =============================================================================

# Google Analytics (EXEMPLE FACTICE)
GA_TRACKING_ID=G-ABC123XYZ

# Prometheus
PROMETHEUS_PORT=9090

# Grafana
GRAFANA_PORT=3001
GRAFANA_ADMIN_PASSWORD=admin

# New Relic (EXEMPLE FACTICE)
NEW_RELIC_LICENSE_KEY=abc123xyz789exemple
NEW_RELIC_APP_NAME=Enseignement

# =============================================================================
# 🐙 GIT / VERSION CONTROL
# =============================================================================

# URL du dépôt
GIT_REPO_URL=https://github.com/VotreUser/VotreRepo

# Branche par défaut
GIT_BRANCH_DEFAULT=main

# Token GitHub (EXEMPLE FACTICE - générer dans GitHub Settings)
GITHUB_TOKEN=ghp_abc123xyz789exemple40caracteres

# Identité Git
GIT_USER_NAME=VotreNom
GIT_USER_EMAIL=votre-email@example.com

# =============================================================================
# 🧪 TESTS
# =============================================================================

# Base de données de test (séparée !)
TEST_DATABASE_URL=postgresql://test_user:test_password@localhost:5432/test_db

# Désactiver emails en tests
TEST_DISABLE_EMAILS=true

# Seed aléatoire (reproductibilité)
TEST_RANDOM_SEED=42

# =============================================================================
# 🐳 DOCKER / CONTENEURISATION
# =============================================================================

# Nom du projet Docker Compose
COMPOSE_PROJECT_NAME=enseignement

# Fichier docker-compose
COMPOSE_FILE=docker-compose.yml

# Variables pour services Docker
POSTGRES_USER=db_user
POSTGRES_PASSWORD=db_password
POSTGRES_DB=app_db

REDIS_PASSWORD=

# =============================================================================
# 🔧 VARIABLES SPÉCIFIQUES AU PROJET
# =============================================================================

# Fonctionnalités expérimentales
ENABLE_EXPERIMENTAL_FEATURES=false

# Pagination : éléments par page
MAX_ITEMS_PER_PAGE=50

# Taille max upload (Mo)
MAX_UPLOAD_SIZE_MB=10

# Expiration lien reset password (minutes)
PASSWORD_RESET_EXPIRY=60

# Mode maintenance
MAINTENANCE_MODE=false

# =============================================================================
# 📝 INSTRUCTIONS D'UTILISATION
# =============================================================================
#
# 1. PREMIÈRE INSTALLATION :
#    cp .env.example .env
#
# 2. ÉDITER .env :
#    - Remplacer TOUTES les valeurs factices par vos vraies valeurs
#    - Générer de vraies clés secrètes (voir commandes dans .env)
#    - Configurer les services externes que vous utilisez
#
# 3. VÉRIFIER .gitignore :
#    echo ".env" >> .gitignore
#
# 4. VALIDER LA CONFIGURATION :
#    - Tester le démarrage de l'application
#    - Vérifier les connexions DB, Redis, APIs
#    - Consulter les logs pour erreurs de configuration
#
# 5. PRODUCTION :
#    - Utiliser des secrets différents de dev/staging
#    - Désactiver DEBUG=false
#    - Activer FORCE_HTTPS=true
#    - Utiliser un gestionnaire de secrets (Azure Key Vault, AWS Secrets Manager)
#    - Ne jamais exposer ce fichier publiquement
#
# 6. MAINTENANCE :
#    - Documenter les nouvelles variables dans ce fichier
#    - Mettre à jour .env.example quand vous ajoutez des variables
#    - Synchroniser avec l'équipe après modifications
#
# =============================================================================
#
# 📚 RESSOURCES :
#   - Twelve-Factor App : https://12factor.net/config
#   - OWASP Secrets Management : https://cheatsheetseries.owasp.org/cheatsheets/Secrets_Management_Cheat_Sheet.html
#   - python-dotenv : https://pypi.org/project/python-dotenv/
#   - dotenv (Node.js) : https://www.npmjs.com/package/dotenv
#
# 🆘 SUPPORT :
#   - Issues : https://github.com/VotreUser/VotreRepo/issues
#   - Documentation : https://docs.votreprojet.com
#
# =============================================================================